% Enables the [H] float specifier for per-figure pinning via raw LaTeX blocks.
% Language-neutral. NOTE: does NOT change default float behavior — only loads
% the package so individual figures can opt in to [H] when needed.
\usepackage{float}
